# Core dependencies — pinned for reproducibility.
# Generated from pyproject.toml. Run `pip install -e .[dev]` for development.

pydantic==2.9.2
python-dotenv==1.0.1
google-generativeai==0.8.3

# UI layer (Step 8+)
streamlit==1.41.1
pandas==2.2.3
altair==5.4.1

# Optional providers (Step 9+) — uncomment to enable
# openai==1.54.3
# anthropic==0.39.0
